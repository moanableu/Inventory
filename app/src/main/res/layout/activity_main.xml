<?xml version="1.0" encoding="utf-8"?>
<!--image credit: ChicagoGit.com Bookshelf Icon Png # 326467, detail-->

<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ListView
        android:id="@+id/list_view_book"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp" />

    <LinearLayout
        android:id="@+id/empty_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="56dp"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/empty_inventory_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:paddingLeft="24dp"
            android:src="@drawable/bookshelf" />

        <TextView
            android:id="@+id/empty_inventory_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/empty_inventory_img"
            android:gravity="bottom"
            android:paddingLeft="32dp"
            android:paddingTop="16dp"
            android:text="@string/emtpy_view_text"
            android:textColor="@color/fontColor_charcoal"
            android:textSize="16sp" />
    </LinearLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        android:src="@drawable/add_item" />

</android.support.design.widget.CoordinatorLayout>